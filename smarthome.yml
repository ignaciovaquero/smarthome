server:
  port: 8080
  address: 0.0.0.0

aws:
  region: eu-west-3
  dynamodb:
    endpoint: http://127.0.0.1:8000
    tables:
      control: ControlPlane
      outside: TemperatureOutside
      inside: TemperatureInside

logging:
  verbose: true
